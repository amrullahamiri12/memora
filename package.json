{
  "name": "flashcards",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "setup": "npm install --prefix server && npm install --prefix client && npm run db:migrate --prefix server && npm run db:seed --prefix server",
    "sync:openapi": "cp docs/openapi.yaml client/public/openapi.yaml",
    "build:vercel": "npm run sync:openapi && cd server && npx prisma generate && if [ -n \"$DATABASE_URL\" ]; then npx prisma db push --skip-generate; fi && if [ \"$SEED_ON_BUILD\" = \"true\" ]; then node -r dotenv/config prisma/seed.js; fi && cd ../client && npx vite build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "google-auth-library": "^9.15.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.16.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
